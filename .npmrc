auto-install-peers=false
strict-peer-dependencies=false
# Keep pnpm store on the project drive (e.g. D:\...\Dungeon Master Lite\.pnpm-store)
store-dir=.pnpm-store
# Use hoisted layout to avoid Windows symlink errors (EISDIR with cross-env/cross-spawn)
node-linker=hoisted
