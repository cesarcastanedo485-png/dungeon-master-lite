Update your existing Flask-based Dungeons & Dragons style adventure game bot in Python (text-based, TikTok Live integration with tiktoklive, Grok as DM via Replit AI Integrations/Core plan, max 4 players, !create/!action/!sheet/!party commands, immersive no fourth-wall, XP/leveling 1-20 with simple class skill trees for wizard/cleric/fighter/rogue/hunter, race list: tiefling/dragonborn/human/elf/halfling/gnome, web app tabs for Races/Classes/Active Characters, start/pause/reset buttons, active player management, browser control). Keep all current functionality intact—no breaking changes.

Step-by-step tweaks for the home/adventure page (main index.html or game screen):
- Overall look: Keep it as is (you like it), but enhance with the following additions.
- DM name: Give the DM a custom name that's relevant to the story/session (e.g., "Eldric the Chronicler" for a fantasy tale; make it changeable in code/settings or auto-generated by Grok on start—e.g., prompt Grok for a fitting name based on starting outline). Do not use "D&D Auto DM" or "Grok." Display the DM name in instructions and responses (e.g., "Eldric says:").
- Pre-story instructions: Before the story starts (on the home screen or a pre-game panel), add a section with immersive instructions "from the DM" (e.g., as a formatted text block styled like a scroll or parchment). Include:
  - How to interact: "Type !create [race] [class] [name] in chat to make your character (e.g., !create elf wizard Luna)."
  - Explain commands: !action [description] for actions, !sheet for your character, !party for group list.
  - Mention: "This adventure was crafted by Cesar himself—join the party and shape the tale!"
  - Game rules: Max 4 players, wait for start button, only active players can act, pause to add/substitute.
  - Keep it concise, engaging, and in-character from the DM name.

Add Save and Load Campaign feature:
- Allow saving the current campaign state (party characters, story log, XP/levels, active players, current DM name, game state like location/quest progress) to Replit's db or a JSON file.
- Browser controls: Add "Save Campaign" button with input field to name it (e.g., "Forest Quest"), store multiple named saves.
- "Load Campaign" dropdown/button: List saved campaigns, load selected one (restore all state, resume story).
- Make it "put away" friendly: Save mid-session, load later to continue or manage multiple stories (e.g., switch between "Haunted Forest" and "Mountain Siege").
- Integrate with pause/start: Auto-prompt to save on pause/reset if unsaved changes.

Update Grok DM prompt to include the custom DM name (e.g., "You are [DM_NAME], a fair, immersive Dungeon Master...").

Generate the full updated code, including modifications to app.py (add save/load logic with db/JSON, DM name generation/storage), templates/index.html (add pre-story instructions panel/scroll, save/load buttons/inputs/dropdown), static/style.css (style instructions as parchment/scroll with fantasy font/borders). Ensure everything runs in Replit with "Always On", preview in browser for screen-share. Add comments explaining new features.